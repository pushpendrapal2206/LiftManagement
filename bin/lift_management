#!/usr/bin/env bash
# Add script to run program here.
# Complete bin/setup so that after it is
# run, bin/lift_management can be used to launch
# it.

# This variable contains absolute path of this `lift_management` script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
java -cp $DIR/../target/lift-1.0-SNAPSHOT.jar com.phonepe.lift.api.LiftManagementApplication
